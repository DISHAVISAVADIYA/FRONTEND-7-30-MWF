import React from 'react';
import  ReactDOM  from 'react-dom';
import List from './List';

const abc = ReactDOM.createRoot(document.getElementById('demo'));
abc.render(
    <React.StrictMode>
<List />
     </React.StrictMode>
)
;